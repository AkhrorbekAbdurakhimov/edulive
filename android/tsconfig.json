{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    // expo/tsconfig.base'dagi "node" (node10) eskirgan — Metro'ga mosi "bundler"
    "module": "esnext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  }
}
